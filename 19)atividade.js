 /* 19. Crie o seguinte array: var deuses = ['Aegir', 'Aud', 'Balder'] e exiba um a um 
 com alert() - repita para cada um dos laços de repetição (for, forEach, while,
 do{}while) */

// Executando com For
/*
 var deuses = ['Aegir', 'Aud', 'Balder'];

 for (let i = 0; i < deuses.length; i++) {
     alert(deuses[i]);
 } */

 /* Execução com o While. 
 var deuses = ['Aegir', 'Aud', 'Balder'];

 let i = 0;
while(i < deuses.length) {
    alert(deuses[i]);
    i++;
}

/*  Executando pelo Do While 
var deuses = ['Aegir', 'Aud', 'Balder'];

let i = 0;

do {
    alert(deuses[i]);
    i++;
} while (i < deuses.length) 
 */



