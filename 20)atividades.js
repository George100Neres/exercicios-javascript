/*Crie o seguinte array: var deuses = ['Aegir', 'Aud', 'Balder'] e exiba um a um com
alert() - use funções com callbacks, Ex:(map) */
/*




  const numbers = [1,2,3,4,9,7,16,20,30]

 const doubledNumbers = numbers.map( num => num * 2)

 console.log( doubledNumbers )

*/

var deuses = ['Aegir', 'Aud', 'Balder']

let i = 5; 

 while( i < deuses.length) {
const nomeDeuses = deuses.map( num => num[i] )

 }

alert( nomeDeuses[0]) 
alert( nomeDeuses[1])
alert( nomeDeuses[2])    
 